## 1. 탐색 상태와 뷰 계약

- [x] 1.1 `RecordListView` 단위 테스트에 첫·중간·마지막 묶음의 위치 문구, 이전·다음 비활성 상태, 20·50·100·200 선택지와 로딩·빈 결과·오류 상태를 추가하고 실패하는 테스트로 요구사항을 확인한다.
- [x] 1.2 목록 뷰에 접근 가능한 페이징 영역, 현재 묶음 위치, 항상 렌더링되는 이전·다음 버튼과 묶음 크기 컨트롤 상태를 구현하고 `pnpm --filter @oss-scp/web test`로 확인한다.

## 2. cursor 이력과 요청 생명주기

- [x] 2.1 컴포넌트 테스트에 다음→이전 cursor 요청, 이동 실패 시 마지막 성공 상태 유지, 크기·route 변경 초기화, 취소를 무시한 늦은 응답 경합 사례를 추가해 실패하는 테스트로 상태 전이를 확인한다.
- [x] 2.2 방문 cursor 경로와 성공 위치, 이동 의도를 분리해 앞뒤 이동을 구현하고 조건 변경 시 첫 묶음으로 초기화하며 AbortController와 요청 식별자로 경합을 차단한 뒤 `pnpm --filter @oss-scp/web test`와 `pnpm --filter @oss-scp/web typecheck`로 확인한다.

## 3. 브라우저 통합과 전체 검증

- [x] 3.1 브라우저 fixture 흐름에 앞뒤 이동의 요청 cursor·행 검증, 마지막 묶음 버튼 상태, 묶음 크기 및 route 초기화, 느린 응답 무시를 추가하고 `pnpm test:browser`로 확인한다.
- [x] 3.2 관련 스타일을 페이징 컨트롤의 포커스·비활성 상태가 구분되도록 조정하고 `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`로 전체 변경을 검증한다.
