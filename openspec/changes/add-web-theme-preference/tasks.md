## 1. 테마 결정과 초기화

- [x] 1.1 테마 선택·실제 색상 결정·저장 읽기/쓰기 모듈을 구현하고 system/light/dark, 저장값 없음·손상, 저장소 예외, 시스템 조회 불가 단위 테스트로 확인한다.
- [x] 1.2 `apps/web/index.html`에 첫 표시 전 초기화를 추가하고 런타임과 동일한 입력 행렬 및 앱 모듈 지연 브라우저 검증으로 초기 배경과 렌더 테마 일치를 확인한다.
- [x] 1.3 시스템 변경 구독과 해제 및 세션 선택 처리를 구현하고 시스템 모드 전환·명시 모드 고정·StrictMode 재구독 cleanup 테스트로 확인한다.

## 2. 전역 UI와 색상

- [x] 2.1 App 셸에 레이블이 연결된 테마 아이콘 버튼를 추가하고 키보드 선택·접근 가능한 이름·현재 값·경로 이동 시 유지를 브라우저 테스트로 확인한다.
- [x] 2.2 `style.css`의 고정 색상을 의미 기반 라이트·다크 토큰으로 전환하고 시작·목록·상세·상태·컨트롤·포커스의 실제 사용 색상 조합 대비가 spec 기준을 충족하는지 검증한다.
- [x] 2.3 두 테마에서 서버 성공·실패, 빈 목록·부분 결과·오류, 상세 빈 값·잘못된 값과 모바일 화면을 확인하고 가독성·포커스·레이아웃 검증 결과를 기록한다.

## 3. 회귀 검증과 문서

- [x] 3.1 `scripts/test-browser.mjs`에 새로고침·동일 출처 재방문 복원, OS 변경, 명시 모드 고정, 저장소 실패 시 조작, 주요 화면 테마 일관성 회귀 검증을 추가하고 `pnpm test:browser`로 확인한다.
- [x] 3.2 `docs/client-development.md`에 기본 모드·선택 저장 범위·실패 시 세션 동작·색상 토큰 사용을 문서화하고 실제 구현과 대조한다.
- [x] 3.3 웹 테스트·타입 검사·빌드 및 저장소 lint를 실행하고 개발·배포 자산의 첫 표시 검증 결과를 기록한다. 실행하지 못한 검사는 사유를 남긴다.
- [x] 3.4 main을 다시 확인해 #74 이외 변경이 없는지 검사하고 #86·#87이 먼저 반영되었다면 추가된 컨트롤·행 포커스가 테마 토큰을 사용하는지 통합 검증한다. 저장소 PR 템플릿에 변경·검증을 기록하고 완료 범위가 충족될 때만 `Closes #74`로 연결한다.



최종 사용자 결정: 저장된 선택이 없으면 시스템 테마를 따른다. 태양 버튼을 누르면 다크(달), 달 버튼을 누르면 라이트(태양)로 즉시 전환하고 명시적 선택을 저장한다. 팝오버와 시스템 설정 선택 UI는 제공하지 않는다. 버튼은 전환 대상 이름을 보조 기술에 제공하며 Enter·Space로 조작한다.
