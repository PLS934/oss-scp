## 1. 설정과 등록 API
- [x] 1.1 공개 요약, 선택 설명·enabled 및 메뉴 생략을 지원한다.
- [x] 1.2 비활성을 수집·메뉴·모듈 로딩에서 제외하고 등록 API를 제공한다.
- [x] 1.3 설정 호환성·비활성·메뉴 없음·API 정보 경계 회귀 테스트를 추가한다.

## 2. 홈 화면
- [x] 2.1 등록 목록과 메뉴별 링크, 예외 상태를 구현한다.
- [x] 2.2 선택 정보 누락·복수 메뉴·실패·빈 결과 테스트를 추가한다.

## 3. 검증과 문서
- [x] 3.1 설정과 API 호환성 문서를 갱신한다.
- [x] 3.2 전체 빌드·타입 검사·린트 및 관련 패키지 테스트를 실행한다.
- [x] 3.3 브라우저에서 실제 등록 API·홈 이동·예외 상태·모바일을 검증한다.

검증 결과: `pnpm build`, `pnpm typecheck`, `pnpm lint`, plugin-config 78개·web 72개·api 25개 테스트 및 `pnpm test:browser` 통과. 브라우저 검사는 기존 GitHub Actions의 `pnpm test:browser` 단계에 포함된다.

## 4. 출처 상세와 로컬 CSV 원본
- [x] 4.1 공개 HTTP endpoint·메서드·CSV 파일명을 요약과 홈에 제공하고 정보 노출 경계를 테스트한다.
- [x] 4.2 등록된 활성 로컬 CSV만 원본 다운로드하도록 서버를 구현하고 원본 일치·오류·경로 이탈·크기 제한을 테스트한다.
- [x] 4.3 홈 다운로드 링크와 실제 브라우저 다운로드를 검증하고 문서·미리보기를 갱신한다.

추가 검증: plugin-config 79개·web 77개·api 31개 테스트, API·웹 빌드, 린트, 브라우저 원본 다운로드 바이트 일치 검증 통과. 미리보기 `http://127.0.0.1:5103/`에서 API 주소·CSV 파일명·로컬 원본 링크 표시를 확인했다.

## 5. 홈 표시 정리
- [x] 5.1 빈 설명·원본 안내를 제거하고 제목 옆 조회 아이콘, 제목 아래 메뉴 그룹·이름, 파일명 옆 다운로드 아이콘을 적용한다.
- [x] 5.2 접근성·배치·다운로드 회귀 검증과 미리보기 반영을 완료한다.

화면 정리 검증: web 78개 테스트, 웹 빌드, 린트, 브라우저 회귀·다운로드 검증 통과. 실행 중인 미리보기에서 제목 옆 링크 아이콘과 파일명 옆 다운로드 아이콘 배치를 확인했다.

최신 main(`cce3d4a`) 리베이스 검증: 테마 헤더·번호형 페이지네이션을 유지하고 플러그인 카드 색상을 공통 테마 변수에 연결했다. 전체 빌드·타입 검사·린트, web 119개·plugin-config 79개·api 32개 테스트, 브라우저 회귀 및 OpenSpec strict 검증 통과. 실행 중인 미리보기의 라이트·다크 테마와 로컬 CSV 전용 파일명 표시도 확인했다.

## 6. 목록 정렬
- [x] 6.1 이름 순 정렬과 원래 순서 복원를 구현한다.
- [x] 6.2 정렬 전환·원본 순서 유지·브라우저 표시를 검증한다.

정렬 검증: web 123개 테스트·웹 빌드·린트·브라우저 회귀 통과. 실제 목록 정렬 및 기본 순 복원, 한글·영문 대소문자·자연 숫자 순서·모바일 너비를 확인했다.

- [x] 6.3 안내 아래 목록에 가까운 작고 둥근 이름 순 토글 버튼(아이콘 없음)을 제공하고 재클릭 복원을 검증한다.

토글 검증: 실제 미리보기 및 브라우저 회귀에서 클릭 시 이름 정렬·눌림 상태, 재클릭 시 원래 순서 복원을 확인했다. web 123개 테스트·웹 빌드·린트 통과.
