{
  "name": "@oss-scp/local-csv-source",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "typecheck": "pnpm --filter @oss-scp/csv-reader build && pnpm --filter @oss-scp/plugin-config build && tsc --noEmit",
    "test": "pnpm build && vitest run"
  },
  "dependencies": {
    "@oss-scp/csv-reader": "workspace:*",
    "@oss-scp/plugin-config": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "5.9.3",
    "vitest": "4.1.11"
  }
}
