{
  "name": "@oss-scp/http-collector",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "prebuild": "pnpm --filter @oss-scp/plugin-config build",
    "build": "tsc",
    "pretypecheck": "pnpm --filter @oss-scp/plugin-config build",
    "typecheck": "tsc --noEmit",
    "test": "pnpm build && vitest run --maxWorkers=1"
  },
  "dependencies": {
    "@oss-scp/plugin-config": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "5.9.3",
    "vitest": "4.1.11"
  }
}
